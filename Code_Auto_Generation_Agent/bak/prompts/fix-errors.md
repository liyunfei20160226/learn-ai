# 修复代码错误

## 上下文回顾

{{ORIGINAL_PROMPT}}

## 错误列表

{{ERROR_LIST}}

## 涉及文件的当前内容

{{FILE_CONTENTS}}

## 修复原则

⚠️ **重要提醒**：
1. **必须返回完整文件内容**，不要只返回 diff 或修改片段！
2. 必须使用与架构文档一致的依赖版本
3. ESLint 配置格式必须匹配架构文档中的版本
4. 目录结构遵循架构文档的规范
5. 不要引入架构文档以外的新依赖
6. 只修改与错误相关的文件，不要做无关的重构

{{ENV_INFO}}

## 📌 输出格式要求 - 必须严格遵守！

请修复上述错误，**输出每个需要修改文件的完整内容**。不要只返回 diff 或修改片段！

```backend/app/main.py
from fastapi import FastAPI
...
```

⚠️ 严格规则（不遵守会导致解析失败）：
1. **代码块的第一行必须是完整的文件相对路径** ❗️
2. **绝对不要添加任何语言标记**（如 python、tsx、json 等） ❗️
3. **必须返回完整文件内容**，不要只返回 diff 或修改片段 ❗️
4. 路径后面直接跟内容，不要有空行
5. 每个文件单独一个代码块

❌ 错误（加了语言标记）：
```python
backend/app/main.py
...
```

✅ 正确（第一行就是路径，完整文件内容）：
```backend/app/main.py
...
```

不符合格式的输出将被拒绝并重试！

# 修复代码错误

上面的实现运行质量检查后发现以下错误：

{% for error in errors %}
- {{ error }}
{% endfor %}

## 你的任务

请修复这些错误。保持原有的实现思路不变，只修复导致错误的问题。修复完成后重新生成完整的正确代码。

## 输出格式要求

你必须使用以下markdown代码块格式输出**每个需要修改的文件**：

```
filepath
file content here
```

每个文件一个代码块。第一行必须是**完整的文件相对路径**。不要省略路径。

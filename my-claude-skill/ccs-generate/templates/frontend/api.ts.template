/**
 * API 客户端
 * 自动生成由 ccs-generate
 */

import { {{ interface_name }}, {{ interface_name }}Create, {{ interface_name }}Update } from '../types/{{ feature_name }}';

const API_BASE = process.env.NEXT_PUBLIC_API_URL || 'http://localhost:8000/api';

/**
 * 获取列表
 */
export async function get{{ interface_name }}List(skip: number = 0, limit: number = 100): Promise<{{ interface_name }}[]> {
  const response = await fetch(`${API_BASE}/{{ table_name }}/?skip=${skip}&limit=${limit}`);
  if (!response.ok) {
    throw new Error('Failed to fetch {{ feature_name }} list');
  }
  return response.json();
}

/**
 * 根据ID获取
 */
export async function get{{ interface_name }}ById(id: number): Promise<{{ interface_name }}> {
  const response = await fetch(`${API_BASE}/{{ table_name }}/${id}`);
  if (!response.ok) {
    throw new Error('Failed to fetch {{ feature_name }}');
  }
  return response.json();
}

/**
 * 创建
 */
export async function create{{ interface_name }}(data: {{ interface_name }}Create): Promise<{{ interface_name }}> {
  const response = await fetch(`${API_BASE}/{{ table_name }}/`, {
    method: 'POST',
    headers: {
      'Content-Type': 'application/json',
    },
    body: JSON.stringify(data),
  });
  if (!response.ok) {
    throw new Error('Failed to create {{ feature_name }}');
  }
  return response.json();
}

/**
 * 更新
 */
export async function update{{ interface_name }}(id: number, data: {{ interface_name }}Update): Promise<{{ interface_name }}> {
  const response = await fetch(`${API_BASE}/{{ table_name }}/${id}`, {
    method: 'PUT',
    headers: {
      'Content-Type': 'application/json',
    },
    body: JSON.stringify(data),
  });
  if (!response.ok) {
    throw new Error('Failed to update {{ feature_name }}');
  }
  return response.json();
}

/**
 * 删除
 */
export async function delete{{ interface_name }}(id: number): Promise<void> {
  const response = await fetch(`${API_BASE}/{{ table_name }}/${id}`, {
    method: 'DELETE',
  });
  if (!response.ok) {
    throw new Error('Failed to delete {{ feature_name }}');
  }
}

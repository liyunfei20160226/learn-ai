---
name: Git Push Rule
description: Only push code when explicitly told to do so by the user
type: feedback
---

Rule: **Do NOT push code to remote unless the user explicitly tells you to push.**

Why: User wants to control when code is pushed.

How to apply:
- After committing changes locally, do NOT run `git push` unless user says to push
- Wait for user instruction before pushing
